
---
// src/pages/admin.astro

// This component runs on the server (build time) or request time.
// It uses Astro.redirect to tell the browser to go to the full path.
// This ensures that when the user types 'yoursite.com/admin', 
// they are smoothly sent to the correct TinaCMS dashboard file.
return Astro.redirect('/admin/index.html');
---