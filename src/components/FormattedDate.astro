---
interface Props {
  date?: Date;
}

const { date } = Astro.props;
---

{
  date && (
    <time datetime={date.toISOString()}>
      {date.toLocaleDateString("en-GB", {
        month: "long",
        day: "2-digit",
        year: "numeric",
        timeZone: "UTC",
      })}
    </time>
  )
}
